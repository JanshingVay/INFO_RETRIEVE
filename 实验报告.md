# 中文信息检索系统实验报告

## 一、实验概述

本实验实现了一个面向中文科技新闻的本地信息检索系统。系统从公开网页采集文本数据，经过清洗、分词和去重后构建倒排索引，再基于 TF-IDF 向量空间模型完成自然语言查询和相关度排序。用户输入查询后，系统返回按得分排序的文档列表，并展示标题、摘要片段、URL、日期和相关度分数。

在文本检索主流程之外，项目还实现了 BM25 排序、相关反馈、人工评价、可视化图表和小规模多媒体检索。命令行版本便于逐步调试各模块，Streamlit 图形界面则用于集中展示系统状态、检索结果和评价数据。整个系统没有直接接入 Elasticsearch、Whoosh 等现成搜索框架，而是保留了从语料采集到索引构建、从词项权重计算到结果排序的关键过程。这样虽然多写了一些基础代码，但也更容易看清一个信息检索系统内部到底是如何工作的。

### 小组分工

| 成员 | 学号 | 主要负责内容 | 具体工作 |
|---|---|---|---|
| 成员1 |  | 数据采集与预处理 | 负责爬虫数据源整理、异步爬虫运行、文档清洗、去重检查和 documents.json 数据维护。 |
| 成员2 |  | 索引与检索算法 | 负责中文分词、倒排索引构建、TF-IDF 向量空间模型、BM25 排序模型和检索结果调试。 |
| 成员3 |  | 系统界面与实验评价 | 负责 Streamlit 图形界面、人工评价数据整理、可视化图表、多媒体检索测试和实验报告整合。 |

## 二、实验环境与运行方式

### 2.1 实验环境

操作系统：Windows

开发语言：Python

虚拟环境：使用 python -m venv .venv 创建

主要依赖库：requests、beautifulsoup4、aiohttp、jieba、lxml、numpy、matplotlib、streamlit、pandas、torch、transformers、sentence-transformers、opencv-python、pytest 等。

### 2.2 环境配置与启动

进入项目目录：

```bash
cd E:\BUPTStudy\课程\信息与知识获取\final\lab2\INFO_RETRIEVE
```

创建并激活虚拟环境：

```bash
python -m venv .venv
.venv\Scripts\Activate
```

安装依赖：

```bash
pip install -r requirements.txt
```

命令行入口：

```bash
python main.py
```

图形界面入口：

```bash
streamlit run app.py
```

命令行菜单主要用于执行爬虫、索引构建、VSM 检索、BM25 检索、算法对比、人工评价和图表生成。常用运行顺序是：先爬取文档并生成 documents.json，再构建 inverted_index.json，然后分别测试 VSM 和 BM25 检索，最后进入人工评价和图形界面查看结果。

## 三、数据集构建与预处理

### 3.1 数据来源与爬虫实现

本实验采集的是中文科技类新闻和资讯页面。爬虫配置了多个公开科技站点，包括 IT之家、36氪科技、新华网科技、人民网科技、凤凰网科技、网易科技、新浪科技等。选择科技新闻作为语料，主要是因为这类页面通常包含标题、正文、日期和 URL，方便转化为结构化文档；同时，科技新闻中包含人工智能、芯片、机器人、量子计算、新能源等稳定主题，适合作为查询测试对象。

爬虫模块位于 async_crawler.py，使用 asyncio 和 aiohttp 实现异步请求。每个数据源在 config.py 中配置列表页、文章链接特征、标题选择器、正文选择器和日期匹配规则。爬取过程中会轮换 User-Agent；遇到超时或连接错误时，程序会进行重试，而不是直接丢弃整个数据源。

与同步 requests 写法相比，异步爬虫更适合处理“列表页加文章页”的采集场景。新闻页面的主要耗时往往在网络等待，异步请求可以减少等待时间。不过本项目并没有追求大规模抓取，而是把数据规模控制在本地检索和人工评价都能承受的范围内。

### 3.2 数据清洗、去重与存储

data_cleaner.py 负责数据清洗。清洗过程包括：去除 HTML 标签、广告、版权声明和导航文本；过滤正文长度过短的无效文档；使用 URL 精确去重；使用标题相似度进行近重复去重，阈值为 0.85；最后统一空白字符并截断过长标题。

当前 documents.json 中共保存 739 篇中文科技类文档。检查结果显示，重复 URL 数为 0，重复标题数为 0。文档总数、唯一 ID 数、索引文档数均为 739，说明清洗后的文档集合和索引文件能够对应起来。

图1展示了系统状态页面。可以看到，文档数、唯一文档 ID 数和索引文档数保持一致，说明文档库与倒排索引已经完成构建。

（此处插入截图：图1 系统状态页面）

单篇文档的字段包括 id、title、text、url、date、source 和 tokens 等。文档结构示例如下：

```json
{
  "id": 0,
  "title": "文档标题",
  "text": "清洗后的正文内容",
  "url": "原始网页地址",
  "date": "发布日期",
  "source": "数据来源"
}
```

倒排索引保存在 data/inverted_index.json，人工评价数据保存在 data/feedback.json 和 data/eval_queries.json，多媒体索引保存在 video_index.pkl 和 video_metadata.json 中。

### 3.3 数据来源分布

根据 source 字段统计，当前语料来源如下：

1. IT之家：368 篇。
2. 新浪科技：142 篇。
3. 网易科技：91 篇。
4. 凤凰网科技：68 篇。
5. 36氪科技：31 篇。
6. 新华网科技：28 篇。
7. 人民网科技：3 篇。
8. 未标注来源：8 篇。

从来源分布看，IT之家和新浪科技占比较高，语料整体偏向信息技术、互联网产品、终端设备和产业新闻。这种分布并不完全均匀，但对本实验是可以接受的：它能保证系统在信息科技主题上有足够文档可检，同时不同站点的写法差异也能给分词和排序带来一定真实噪声。

### 3.4 中文分词与 tokens 生成

中文文本不能像英文一样直接按空格切分，因此系统使用 jieba 进行分词。preprocessor.py 的处理流程是：先去除 HTML 标签和特殊符号，再使用 jieba.lcut 分词，随后去除停用词、纯数字和无意义的单字符非中文词。系统会把标题和正文合并后生成文档 tokens 字段。

例如，查询“人工智能技术发展”经过分词后，可能得到“人工智能、技术、发展”等词项。后续倒排索引和向量计算都建立在这些词项之上。分词结果不是绝对完美的，尤其是一些新技术词和品牌词可能会被切开，但对科技新闻的基本检索已经够用。

## 四、系统设计与技术选型

系统整体流程可以概括为：

数据采集 -> 数据清洗 -> 中文分词 -> 倒排索引构建 -> 查询分词 -> 候选文档召回 -> 相关度计算 -> 结果排序与展示 -> 人工评价与反馈优化。

主要模块如下：

1. config.py：集中配置文件路径、爬虫参数、数据源和清洗参数。
2. async_crawler.py：负责网页采集和初步解析。
3. data_cleaner.py：负责正文清洗、过滤和去重。
4. preprocessor.py：负责中文分词、停用词过滤和 tokens 生成。
5. inverted_index.py：负责倒排索引构建、IDF 统计和索引持久化。
6. vsm.py：实现 TF-IDF 向量空间模型和余弦相似度排序。
7. bm25.py：实现 BM25 排序模型。
8. relevance_feedback.py：记录用户评分，并用于查询扩展和结果加权。
9. evaluator.py：提供人工评价流程和 Precision@10、Recall 计算。
10. visualization.py：生成评价图表和索引统计图表。
11. multimodal_retrieval.py：实现文本到图像、文本到视频的跨模态检索尝试。
12. main.py 和 app.py：分别提供命令行界面和图形界面。

技术选型上，项目没有直接套用成熟搜索框架，而是自己实现了倒排索引和排序模型。倒排索引、TF-IDF、向量空间模型和 Precision/Recall 是经典信息检索教材中反复讨论的基础方法，本项目参考 Manning 等人的整理实现了这些核心环节。爬虫部分选用 asyncio 和 aiohttp，是为了在代码规模可控的情况下减少网络等待；中文分词选用 jieba，是因为它安装轻、使用简单，对新闻文本的效果比较稳定。图形界面选择 Streamlit，主要是它可以直接复用 Python 代码和本地 JSON 文件，不需要额外搭建前后端。

## 五、索引与检索模型

### 5.1 倒排索引

倒排索引采用“词项到文档”的结构。每个词项对应一组文档，并记录它在对应文档中出现的次数：

```text
词项 -> { 文档ID1: 出现次数, 文档ID2: 出现次数, ... }
```

例如：

```text
人工智能 -> {12: 3, 45: 1, 108: 2}
```

这表示“人工智能”在 12 号文档中出现 3 次，在 45 号文档中出现 1 次，在 108 号文档中出现 2 次。倒排索引可以减少检索时需要遍历的文档数量。用户查询某个词时，系统可以直接从索引中找到包含该词的候选文档，而不必扫描全部正文。

inverted_index.py 中的 IDF 计算公式为：

$$
IDF(t)=\log_{10}\frac{N}{df(t)+1}+1
$$

其中，N 是文档总数，df(t) 是包含词项 t 的文档数。常见词会出现在许多文档中，df 较大，IDF 较低；更有区分度的词出现在较少文档中，IDF 较高。当前 inverted_index.json 中保存了 739 篇文档的索引，词项数为 32827，文档长度记录数为 739。

### 5.2 TF-IDF 向量空间模型

VSM 是系统的基本文本检索模型，实现在 vsm.py 中。它把查询和文档都表示为词项向量，再用余弦相似度衡量二者接近程度。词项权重由 TF 和 IDF 相乘得到：

$$
weight(t,d)=TF(t,d)\times IDF(t)
$$

查询向量 q 和文档向量 d 的相似度计算公式为：

$$
\cos(q,d)=\frac{q\cdot d}{\lVert q\rVert \times \lVert d\rVert}
$$

余弦相似度关注向量方向，而不是单纯的向量长度。因此它可以在一定程度上减弱长文档天然占优的问题。系统在检索时先根据查询词从倒排索引中取候选文档，再计算候选文档和查询之间的余弦相似度，最后按得分降序返回结果。

VSM 检索流程如下：

1. 用户输入自然语言查询。
2. 系统对查询进行清洗、分词和停用词过滤。
3. 根据查询词从倒排索引中取候选文档。
4. 计算查询的 TF-IDF 向量。
5. 计算候选文档的 TF-IDF 向量。
6. 使用余弦相似度打分。
7. 按得分降序输出前若干篇文档。

### 5.3 BM25 排序模型

BM25 实现在 bm25.py 中。它和 VSM 使用同一份倒排索引，但打分方式不同。BM25 的排序思想来自 Robertson 和 Zaragoza 对概率相关模型的总结，本项目采用其中常见的 k1 和 b 参数形式。公式如下：

$$
score(D,Q)=\sum_{q_i\in Q} IDF(q_i)\cdot
\frac{f(q_i,D)(k_1+1)}
{f(q_i,D)+k_1(1-b+b\frac{|D|}{avgdl})}
$$

其中，f(qi, D) 表示查询词 qi 在文档 D 中出现的次数，|D| 是文档长度，avgdl 是平均文档长度。k1 控制词频饱和程度，本系统取 1.5；b 控制文档长度归一化程度，本系统取 0.75。

BM25 的一个直观特点是：同一个词出现次数越多，分数会提高，但提高幅度会逐渐变小。这样可以避免某篇文档因为重复出现某个词而获得过高排名。实际测试中，BM25 和 VSM 的 Top 结果有时会不同，这种差异正好可以用来分析不同排序模型的偏好。

### 5.4 检索结果展示

系统支持自然语言查询。用户可以输入“人工智能”“芯片”“量子计算”“机器人”“新能源”等查询语句。返回结果按相关度分数降序排列，每条结果包含相关度分数、文档标题、主要匹配内容或摘要片段、原始 URL 和文档日期。

摘要片段由 vsm.py 和 bm25.py 中的 snippet 逻辑生成。系统会在正文中查找查询词出现的位置，并截取附近文本作为主要匹配内容。如果没有找到明确位置，则返回正文开头的一小段内容。

图2展示了文本检索页面。查询词经过分词后进入 VSM 或 BM25 检索流程，返回结果中包含相关度分数、标题、摘要片段、URL 和日期。

（此处插入截图：图2 文本检索结果页面）

## 六、人工评价与相关反馈

### 6.1 人工评价方式

人工评价模块位于 evaluator.py。系统预设了 10 个科技主题查询，包括人工智能技术发展、5G 通信网络、新能源汽车、芯片半导体、大数据云计算、航天探索、环境保护、医疗健康、区块链技术、机器人自动化等。

评价时，用户查看每个查询返回的 Top 10 文档，并对结果进行 1 到 5 分评分：

1 分：不相关。

2 分：不太相关。

3 分：一般。

4 分：相关。

5 分：非常相关。

当前 feedback.json 中记录了 91 次评分，涉及 82 篇已评价文档；eval_queries.json 中保存了 10 个评价查询。这个标注规模不算大，但已经能够支撑系统演示 Precision@10、Recall 和反馈优化流程。

图3展示了人工评价页面。该页面能够显示总评分数、已评价文档数、评价查询覆盖情况以及具体查询的反馈记录。

（此处插入截图：图3 人工评价统计页面）

### 6.2 评价指标

本实验使用 Precision@10 和 Recall 观察检索结果质量：

$$
Precision@10=\frac{Top10\text{结果中相关文档数}}{Top10\text{返回文档数}}
$$

$$
Recall=\frac{Top10\text{结果中相关文档数}}{\text{人工标注的相关文档总数}}
$$

Precision@10 更关注用户最先看到的结果是否准确，Recall 更关注相关文档是否被找回。由于人工标注数量有限，这些指标更适合作为系统调试和算法对比的依据，而不是严格意义上的大规模评测结论。

### 6.3 相关反馈

相关反馈模块位于 relevance_feedback.py。用户评分大于等于 4 的文档被视为相关文档，评分小于等于 2 的文档被视为不相关文档，评分记录会保存到 feedback.json。

反馈信息主要用于三个地方：从高评分文档中提取高 IDF 词项进行查询扩展；对历史评分较高的文档在后续排序中给予一定加分；对不相关文档中常见的词项降低权重。这个部分参考了 Rocchio 的查询修正思想，但实现上没有完整重算所有文档向量，而是采用查询扩展和文档加权的轻量方案。

Rocchio 相关反馈模型可以表示为：

$$
q'=\alpha q+\beta\cdot \overline{D_r}-\gamma\cdot \overline{D_{nr}}
$$

这种实现成本较低，也比较容易观察反馈前后的结果变化。

## 七、图形界面、多媒体检索与可视化

### 7.1 图形界面

app.py 使用 Streamlit 实现图形化界面。界面包括五个页面：

1. 系统状态：显示文档数、唯一 ID 数、中文文档数、索引文档数、词项数、人工评分数等。
2. 文本检索：支持选择 VSM/TF-IDF 或 BM25，支持设置返回数量，并展示分词结果和检索结果。
3. 多媒体检索：展示已索引视频数量，并支持文本到视频的语义检索。
4. 人工评价：展示评价查询、相关文档数量和反馈统计。
5. 可视化图表：读取 data/charts 下的图表文件并展示。

界面中还加入了“刷新数据缓存”按钮。重新爬虫或重建索引后，可以通过该按钮清空 Streamlit 缓存，使页面显示的数据和本地 JSON 文件保持一致。整理到 Word 时，可以插入系统状态页面、文本检索页面、人工评价页面和可视化图表页面截图。

### 7.2 多媒体检索探索

multimodal_retrieval.py 实现了一个小规模跨模态检索模块。该模块参考 CLIP 将文本和图像映射到同一语义空间的思路，在项目中扩展到视频帧表示：系统使用 Jina CLIP v2 将文本和视频帧映射到同一语义空间，再使用余弦相似度完成文本到视频的匹配。这个模块不承担主检索任务，但可以展示同一套“向量表示加相似度计算”的思想在非文本数据上的延伸。

视频处理流程如下：读取 data/videos 目录下的视频文件；使用 OpenCV 按一定间隔抽取视频帧；将视频帧输入视觉编码模型，得到帧向量；对同一视频的帧向量求平均，得到视频向量；将用户输入的文本查询编码为文本向量；最后计算文本向量和视频向量之间的余弦相似度。

当前 data/videos 中有 cat.mp4、dog.mp4、fish.mp4 三个示例视频，video_metadata.json 中保存了 3 条视频元数据。样例规模较小，但流程是完整的：视频可以被索引，文本查询可以得到对应的视频排序结果。

图4展示了多媒体检索页面。该页面用于展示已索引的视频或图片数量，并根据用户输入的语义查询返回相似的多媒体对象。

（此处插入截图：图4 多媒体检索结果页面）

### 7.3 可视化图表

visualization.py 负责生成图表，输出目录为 data/charts。当前已有图表文件包括：

1. precision_comparison.png：展示 VSM 与 BM25 的 Precision@10 对比。
2. pr_curves_comparison.png：展示不同查询下的 Precision-Recall 曲线。
3. algorithm_radar.png：从多个维度比较算法表现。
4. index_statistics.png：展示文档数、词项数和倒排记录等索引统计信息。
5. crawl_performance.png：展示同步与异步爬取方式的性能差异。

这些图表不是为了装饰页面，而是为了让数据规模、算法对比和系统状态更容易被检查。报告中优先插入 index_statistics.png 和检索界面截图即可；其他图表可以作为附加材料展示。

图5展示了可视化图表页面。图表可以辅助说明索引规模、算法对比和评价指标变化情况。

（此处插入截图：图5 可视化图表页面）

## 八、系统测试与实验结果

### 8.1 数据与索引一致性测试

检查命令如下：

```bash
python -c "import json; docs=json.load(open('data/documents.json',encoding='utf-8')); idx=json.load(open('data/inverted_index.json',encoding='utf-8')); print(len(docs)); print(len(set(d['id'] for d in docs))); print(idx.get('doc_count')); print(len(idx.get('doc_lengths',{})))"
```

检查结果为：文档总数 739，唯一 ID 数 739，索引文档数 739，文档长度记录数 739。

### 8.2 去重测试

检查 URL 和标题重复情况：

```bash
python -c "import json, collections; docs=json.load(open('data/documents.json',encoding='utf-8')); print(sum(c-1 for c in collections.Counter(d.get('url','') for d in docs).values() if c>1)); print(sum(c-1 for c in collections.Counter(d.get('title','') for d in docs).values() if c>1))"
```

检查结果为：重复 URL 数为 0，重复标题数为 0。

### 8.3 检索与界面测试

分别使用以下查询测试 VSM 和 BM25：

```text
人工智能
芯片
量子计算
机器人
新能源
```

测试时主要观察四点：结果是否按相关度排序，摘要是否包含主要匹配内容，URL 和日期是否正常展示，不同算法的前几条结果是否存在合理差异。

图形界面测试通过以下命令启动：

```bash
streamlit run app.py
```

在浏览器中检查系统状态、文本检索、多媒体检索、人工评价和可视化图表页面。系统状态页应显示 739 篇文档和 739 个索引文档。

### 8.4 单元测试

项目 tests 目录下包含以下单元测试：

1. test_preprocessor.py：测试文本清洗、中文分词、停用词过滤、数字过滤和文档批量处理。
2. test_inverted_index.py：测试倒排索引构建、postings 获取、TF/IDF 计算、索引保存和加载。
3. test_bm25.py：测试 BM25 初始化、平均文档长度计算、IDF 计算、单词查询、多词查询、摘要生成和空查询处理。

运行命令为：

```bash
pytest tests/ -v
```

这些测试不能替代人工评价，但可以帮助发现基础模块中的明显逻辑问题。例如，BM25 多词查询测试会检查同时包含多个查询词的文档是否排在更靠前的位置；倒排索引测试会检查词项是否只出现在正确的文档列表中。

### 8.5 结果分析

当前系统状态如下：

1. 文档总数：739 篇。
2. 唯一文档 ID：739 个。
3. 重复 URL：0 个。
4. 重复标题：0 个。
5. 倒排索引文档数：739。
6. 文档长度记录数：739。
7. 索引词项数：32827。
8. 人工评分次数：91 次。
9. 评价查询数量：10 个。
10. 视频元数据：3 条。

从数据集看，739 篇文档能够覆盖多个科技主题，又不会让本地检索变得笨重。对于算法验证、人工评价和界面展示，这个规模比较均衡。

从索引结果看，32827 个词项说明语料中包含较丰富的主题词和新闻实体。倒排索引文档数与文档库数量一致，也说明索引构建没有遗漏文档。

从排序结果看，VSM 和 BM25 的差异是有价值的。VSM 更直接体现 TF-IDF 权重和余弦归一化的效果；BM25 则更强调词频饱和和文档长度归一化。在短查询场景下，BM25 往往会把查询词更集中出现的文档排到前面，而 VSM 对整体向量分布更敏感。

从人工评价看，91 次评分和 10 个评价查询已经形成了一套可复用的反馈数据。它既能用于计算评价指标，也能作为相关反馈模块的输入。虽然标注规模还不大，但足以展示从“系统排序”到“人工判断”，再到“反馈优化”的闭环。

从界面展示看，Streamlit 页面把文档数、索引数、检索结果、评价统计和图表放在同一套界面里，比单纯命令行输出更直观。尤其是在演示时，系统状态页可以快速证明当前数据和索引是一致的。

## 九、合规考虑、不足与改进方向

本项目的数据采集和系统实现主要做了以下控制：数据只来自公开网页，不采集用户隐私数据；爬虫控制在较小规模内，避免无意义的大量请求；数据保存在本地，不上传到第三方服务器；通过去重减少重复存储和重复计算；多媒体模型采用懒加载，只有实际使用时才加载；检索阶段主要依赖本地索引，减少运行时的外部服务依赖。

这些处理不算复杂，但对于课程项目已经比较必要。尤其是爬虫部分，如果只追求数量，很容易带来重复页面、无效正文和过多请求；本项目更倾向于控制规模，把重点放在数据质量和检索链路完整性上。

当前系统仍有一些可以继续改进的地方。第一，数据源以科技新闻站点为主，不同网站的页面结构差异较大，正文抽取偶尔会受到网页模板影响。后续可以选择结构更稳定的专题站点，或者为每个站点编写更细的正文抽取规则。第二，人工评价规模还不够大。91 次评分能够证明系统具备人工评价能力，但如果要更严谨地比较 VSM、BM25 和反馈优化效果，还需要更多查询和更多标注结果。第三，多媒体检索目前只有 3 个示例视频。它可以展示跨模态检索流程，但不能代表大规模多媒体检索效果。第四，BM25 和反馈模块的参数目前采用常见设置，后续可以基于评价集调整 k1、b、反馈权重和 top_k，观察不同参数对 Precision@10 的影响。

本实验完成了一个面向中文科技新闻的本地信息检索系统。系统从网页采集开始，经过清洗、分词、索引构建，再到 VSM 检索、BM25 对比、人工评价和图形界面展示，形成了比较完整的信息检索流程。实际实现中，最容易被低估的是数据处理部分。正文抽取不干净会直接污染分词结果，重复文档会影响评价，日期和 URL 缺失也会降低结果展示质量。相比之下，VSM 和 BM25 的公式并不复杂，真正影响系统体验的往往是这些前处理细节。

## 十、参考文献

1. Manning, C. D., Raghavan, P., & Schütze, H. (2008). Introduction to Information Retrieval. Cambridge University Press.
2. Robertson, S., & Zaragoza, H. (2009). The Probabilistic Relevance Framework: BM25 and Beyond. Foundations and Trends in Information Retrieval, 3(4), 333-389.
3. Rocchio, J. J. (1971). Relevance Feedback in Information Retrieval. In The SMART Retrieval System: Experiments in Automatic Document Processing.
4. Radford, A., Kim, J. W., Hallacy, C., et al. (2021). Learning Transferable Visual Models From Natural Language Supervision. ICML 2021.
